# Mage-VL：中文简版只问物体及是否手机（2026-09-18）

## 结论

同样5例、同样native20视频，简版提示使n03正确认出黑色圆柱杯子并最终明确不是手机，n09认出纸张但对是否手机仍不确定；p03/p07明确手机，p01认为像智能手机但不能完全确认。相较上一轮附业务分类的描述提示，两个非手机对照的物体描述有明显改善，值得继续研究，不应据上一轮就否定模型潜力。

这是认物诊断，不是事件过滤测试：没有业务标签、没有自动排除，也没有上线改动。尤其纸张回答不确定、杯子文字内部有先不确定后确定的转折，不能直接把这些自然语言当成安全过滤器。3个真手机未被明确否定，但不等于已经验证新报警策略的零漏报。

## 用户确认的唯一提示

```text
请结合连续视频，描述目标人物手中拿着的物体是什么，并判断是否是手机。看不清时请说明无法确定，不要猜测。
```

没有添加报警、保留/过滤、主动使用、通话、手机正反面或拍屏规则，也没有LABEL输出约束。提示原文直接作为视频用户消息，未另外翻译或追加英文系统角色。max_new_tokens=112，greedy，与上一轮描述生成预算相同。

## 范围和输入

用户批准先测同样杯子、纸张及3例真手机。冻结顺序n03、n09、p01、p03、p07，每例仅native20生成一次，不走focus/early，因为本轮没有事件过滤判定；没有把诊断输出伪装成完整三次业务复核的最终结果。

同一Mage权重、同一生产模块私有快照、同一BF16 CPU/GPU offload加载，GPU权重上限3800MiB、CPU映射上限24GiB。native20帧、约5秒、448²、无绘制框，与上一轮输入一致；AVI SHA、每帧RGB SHA、grid20断言通过，5个处理后像素张量SHA分别与上一轮baseline native完全一致。每次实际传processor的视频参数并执行generate；本轮没有额外视觉注入hook审计。原有合成序号时间戳问题仍未修正，两轮沿用相同处理路径。

原始回答与正常EOS完整性保存，不添加自动手机/非手机解析或业务过滤。完整性只表示非空且EOS，不代表语义正确。固定工程对照和真实手机组别未重标；这些材料不是独立盲评集。

## 完整回答与结果

| 样例 | 已知组别 | 简版认物结果（人工阅读原文） | token数／耗时 |
| --- | --- | --- | --- |
| n03 | 杯子工程对照 | 黑色圆柱杯子／像保温杯，最终明确非手机 | 79／68.677秒 |
| n09 | 纸张工程对照 | 认出纸，但是否手机仍无法判断 | 17／15.420秒 |
| p01 | 真实手机 | 带屏矩形电子设备，像智能手机，仍不能完全确认 | 62／50.253秒 |
| p03 | 真实手机 | 明确手机 | 10／9.478秒 |
| p07 | 用户确认的非通话手机使用 | 明确手机，不再添加贴耳通话描述 | 10／9.490秒 |

### n03

```text
目标人物手中拿着的物体是一个黑色的圆柱形杯子，看起来像是一个保温杯。根据视频中观察到的特征，无法确定是否是手机，因为手机通常有屏幕或特定的形状，而这个物体是一个圆柱形的杯子，这与手机的典型形状和功能不符。因此，可以确定这个物体不是手机，而是一个保温杯。
```

杯子类别认对，“保温杯”细分类未经独立确认。文字先说无法确定又最终确定，仍有不一致；目前只是比上一轮有屏矩形/圆柱贴耳/早期手机的幻觉更贴近实际，不据此上线。

### n09

```text
目标人物手中拿着的物体是一张纸，无法判断是否是手机。
```

纸类别正确，但手机否定并不明确，不把它统计成已成功过滤。

### p01

```text
目标人物手中拿着的物体是一个带有屏幕的矩形物体，看起来像是一个电子设备。根据其大小、形状和屏幕的存在，可以合理地推断这是一个智能手机。然而，由于视频中没有提供足够的信息来明确确认，因此无法100%确定是否是手机。
```

### p03、p07（两例分别生成，相同回答）

```text
目标人物手中拿着的物体是手机。
```

5/5均正常EOS，无截断，总输入100帧，加载32.559秒，单次均值30.664秒。没有重跑多个回答挑最佳。本轮只问身份，没有要求动作/是否通话，因此p07没再说通话不代表已通过通话姿态判别验证。

## 与上一轮的有限对照

上一轮同次“描述＋报警业务类别”方案的n03最终KEEP，native描述有屏矩形，focus圆柱贴耳，early又说手机；n09描述白色智能手机点击滚动。当前n03/n09的类别描述变成杯子/纸，说明输入并非完全没有可识别的信息，提示设计/任务 framing有可挖掘空间。

但当前同时改变任务范围、提示长度、分类框架和语言（上一轮描述提示为英文），并取消条件补看；不能把改进唯一归因于某一句话，更不能把认物结果直接换算为完整过滤准确率。原始20帧业务baseline近期结果均最终保留这5例，其中p01通过early保留；当前没有覆盖同样业务问题，不把p01不确定算成业务变差或改善。

下一步建议先以这句做固定18例认物对照，保留不确定，再研究身份与非通话主动使用的分步核验。是否扩测、是否增加独立第二轮或灰视频控制另行确定。本轮不扩范围、不部署，不用关键词“杯子/纸/通话”直接触发过滤。

## 保护、恢复和留存

TDD先复现中文正常结束被标不完整及CLI不接受controller trial两项失败，再最小实现，4项契约测试通过；包括截断/空文字不完整、原始观察不产生业务decision，以及拒绝任意输出目录。远端help验证root/trial接口后才启动。没有首轮参数失败或模型推理重试。

沿用当前生产复核完成后切换、同一GPU锁、严格8在线/FPS≥76/alerts空保护；600秒子进程、720秒systemd上限、20GiB内存及双重恢复均未放宽。09:11:02请求停Mage，09:14:13请求恢复，控制周期191.38秒，另含恢复冷加载。91条监督记录全部合格，FPS79.4769—80.5170，总GPU占用采样最高5763MiB。systemd Result=success、ExecMainStatus=0。没有重启采集、检测、网页；Mage复核存在已获允许的测试和恢复暂停，不声称全程无中断。

最终签名恢复验收09:14:50 CST：ready=true，真实新生产复核已完成且最近10.028秒，production_errors=0、memory_pressure=false；8在线79.9862FPS、alerts空。唯一GPU计算进程PID2935889与生产MainPID匹配。生产核心SHA256 `83ec549bb9209018bfde16ccbf03f424a1c49000f83881a19122e99b6e5bfede`、手机提示修订 `600887293a7b364de10ee857979dd0713903914cc22212a66ee6c55169b7b993`、手机证据修订及拍屏提示/证据修订均未改变。对外仍 `192.168.104.53:8767`。

完整manifest、原始回答、EOS/token数、校验、保护和恢复记录私有保存在 `/Users/a1/Documents/监控/output/mage-object-identify-20260918`；远端使用既有 `00_staging/mage_frame30_pair_20260917` 内的新日期脚本及独立trial-object-identify-20260918-a。Git仅纳入本报告，不上传媒体、日志、权重或秘密。

brainstorming将范围限定为用户确认的5例离线spike，TDD保护离线输入输出契约，verification-before-completion约束推理及生产恢复结论。脚本为诊断用途，不保留为生产实现；没有生产代码变更。
