# VideoChat3-4B：相同结果排查与补测（2026-09-17）

## 结论

补测76次，排除本次素材复用、视觉特征漏传及顺序串样；模型确实会随视频、提示和标签名称改变回答。旧报警提示与中性描述存在明显矛盾，但中性描述也会误认真实手机、在纯灰画面编造人物。简化标签能改变判定，却同时增加真实手机首轮排除，仍不替换Mage。线上最终签名验收通过，提示、核心源码、对外地址未改。

## 排查范围

回应“结果为什么几乎一样”，不修改线上模型、提示词、解析器、人工标签或网页数据；旧trial-c保留，不改写成新成绩。复用既有18个冻结事件（12手机、6非手机工程对照），448²、native20/focus20/early8、真实源时间戳、固定权重修订、BF16 CPU/GPU offload及同一实验性视觉SDPA适配。

旧结果并非全部相同：17例原始KEEP、p03原始FILTER；其中3例没有LABEL前缀，严格解析为UNCERTAIN。本轮先查数据和内部链路，再做输入/提示对照。

## trial-d：38次诊断

- 18例倒序重测：18/18原始回答与trial-c逐例相同，包括3个前缀缺失，未发现测试顺序串样。p01最后再问一次仍与前面一致。
- 18个不同AVI和18组不同RGB帧，经处理器后仍为18份不同的归一化像素张量、18份不同的投影视觉特征、18份不同的首次输出logits。
- 38/38调用均实际执行视觉编码一次；从语言模型的输入嵌入中抽出全部视频token，与当次投影特征逐项完全相等。未发现视频漏传、同一特征被重复用于所有样例或视觉token未注入。
- 抽查词嵌入、视觉patch投影、投影器pre-norm、语言第0层q投影的各维前3元素，已加载值与safetensors精确相同；输出头仍与词嵌入共享。此为抽查，不冒充全参数逐项验证。
- CUDA BF16、16头×72维、112token/两组的小规模适配对照：相对官方块对角FP32 SDPA，max_abs_error=0.003811、mean_abs_error=0.000324，低于预设0.02。它不证明完整大序列/整网与官方FlashAttention逐位相同。

强制6个非手机focus20：n01/n03给合法KEEP；n02/n04/n07重复PHONE字段，n09缺LABEL前缀，均严格UNCERTAIN。没有合法FILTER。p03补看focus也重复PHONE字段，early8给无前缀FILTER；不能声称补救后产生合法过滤。

### 不带报警预设的自由描述

同一native20，只换为“描述画面中的人物、手中物体和行为，不假定手机或报警”，9次、最长96token。以下仅用于诊断，不将自由描述自动转成业务过滤：

| 样例 | 原报警提示原始倾向 | 中性描述主要内容 |
| --- | --- | --- |
| n01 | KEEP | 小暗色扁长物体，认为像手机；仍有误认/画质歧义 |
| n02 | KEEP | 小白色物体，可能卡片或小电子物品；96token截断 |
| n03 | KEEP | 穿粉色衣服的人拿黑色杯子喝水，然后放杯打字 |
| n04 | KEEP | 白色物体，可能纸张或小册子 |
| n07 | KEEP | 黑衣男子拿红色物体，未可靠确认身份 |
| n09 | KEEP | 站立男子拿纸看，随后转身离开 |
| p01 | KEEP | 误述为蓝色笔、绿色辅助物体/写画，未确认真实手机 |
| p03 | FILTER（非法协议） | 误述为粉色记事本/小册子，未确认真实手机 |
| p07 | KEEP（非法协议） | 粉色长方形有屏物体，认为是手机，低头使用 |

空白对照保留20帧形状/时间，但全部RGB改为127纯灰：旧分类提示给无前缀UNCERTAIN；中性描述却编出“有人坐椅子拿白色物体”。因此链路正常不等于可信识别；存在视觉事实幻觉。总计37/38正常EOS，唯一截断是n02描述，不将其当成完整答复。

上述证据支持：近乎同类输出不是素材复用或视频完全未进模型；既有报警角色/长标签对判定可能有影响，视觉小物体识别本身也不稳定。不能单独归因提示词，也不能从这组数据推断所有场景能力。

## trial-f：38次提示/标签单因素对照

视频及20帧不变；两种改法各跑全18例，另各做一次纯灰对照，38/38正常EOS、视觉编码/注入断言全部通过。

| 改法（均只测native首轮） | 6个非手机 | 12个真实手机 |
| --- | --- | --- |
| 简短中性判定提示，保留旧长标签 | 5合法KEEP；n04无前缀FILTER→严格UNCERTAIN | 10合法KEEP；p07无前缀FILTER、p04无前缀UNCERTAIN→均严格UNCERTAIN |
| 原提示逐字保留，只将长标签改PHONE_USE/NO_PHONE_USE | 4 PHONE_USE；n04/n07两例NO_PHONE_USE | 9 PHONE_USE；p01/p03/p04三例NO_PHONE_USE |

短标签臂18/18格式正确，使用对应短标签协议才能理解其意义；审计runner沿用旧严格解析器，故JSON旧`label`字段均UNCERTAIN，表中是基于原始短标签+EOS的独立语义统计，不是偷偷修改生产解析器。纯灰两臂都无前缀UNCERTAIN，不是合法LABEL。

长标签到短标签也会改变离散判定，说明输出接口名称不是无关紧要的包装。短标签虽改善两个非手机首轮判定，却新增三个真实手机首轮排除；简化提示也出现p07原始错误排除。因此本轮仍不替换Mage。此组没有按新提示/标签执行focus/early条件救援，不能把2/6称作最终业务误报过滤率，也不能把3/12称作最终业务漏报率。

## 保护与最终验收

trial-d模型加载45.643秒，GPU总占用采样最高8709MiB；139条有效保护均8在线、alerts空、79.4778—80.5350FPS。18:43:34请求停Mage，18:48:50请求恢复，控制周期316.48秒；复核暂停还包括冷加载，不声称Mage全程无暂停。随后签名ready及真实生产完成记录确认恢复。

第二组e启动时，实验包装脚本的字符串换行转义错误，编译阶段退出，0个模型分类，不计入模型准确性。原因已通过本地生成代码编译失败复现，改为raw字符串后，生成的job代码及整份诊断代码均通过编译。原失败脚本/日志留存，新尝试使用不同脚本与trial/log路径，不覆盖旧文件。保护流程恢复Mage，并确认生产已完成2次、最近9.068秒、0错误后才进行f。

所有GPU窗口沿用8在线/FPS≥76/alerts空的严格保护、等待当前生产复核结束、同一GPU锁、600秒子进程上限及systemd720秒/20GiB/双重恢复。没有修改保护阈值或重启前台网页、检测、采集。对外仍192.168.104.53:8767。

trial-f加载46.541秒；71条有效保护均8在线/alerts空，79.5866—80.6389FPS，GPU采样峰值8709MiB。18:52:29请求停Mage，18:55:10请求恢复，162.10秒控制周期，另含恢复冷加载。试验d+f合计76次推理，不把e的启动编译错误算成模型测试。

最终新鲜签名验收（2026-09-18 08:04:19 CST）：Mage ready=true，production_admitted=32、已完成生产推理累计423.412秒、最近9.263秒，production_errors=0、active_kind为空、memory_pressure=false；8在线、79.9034FPS、alerts空。v31 release/生产提示/拍屏提示/证据修订未变，生产核心SHA仍83ec549bb9209018bfde16ccbf03f424a1c49000f83881a19122e99b6e5bfede，显卡仅Mage进程约3386MiB。

## 留存

完整逐次输入张量/视觉特征/语言输入/首次logits校验值、原始生成、EOS/截断、权重抽查与CUDA数值对照、jobs及保护日志在私有目录 `/Users/a1/Documents/监控/output/videochat3-recheck-20260917`，远端仍在既有 `00_staging/videochat3_probe_20260917` 的新trial-d等路径。媒体、特征/日志、权重和秘密不进入Git。

本轮遵循systematic-debugging先检验输入和链路，再做单因素诊断；按brainstorming的离线spike处理，未实施生产修复。实验转义修复先复现编译失败，再检查修复后的生成代码；最终结论以新推理和恢复验收为准。
