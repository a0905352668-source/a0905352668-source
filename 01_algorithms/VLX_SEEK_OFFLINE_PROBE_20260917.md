# VLX-Seek 1.5-10B NF4 离线可行性小试验（2026-09-17）

## 结论与边界

用户批准在独立环境中尝试10B量化版本，不替换线上Mage。RTX 3080 10GB上已完成8次有效的单张图像加候选框推理，主视觉、辅助视觉和HFRE区域编码器均确认执行，没有观察到显存溢出。

4个真手机样本全部识别为手机；4个非手机样本仅饮杯被识别为不含手机，另外3个仍误认。速度有进一步试验价值，但当前结果不支持替换Mage或上线过滤。

这是工程小样本，不是随机盲测。每事件仅选一个有候选框的冻结帧，中心优先，缺少中心候选时回退最早帧；缺少全部候选必须中止。`FRAME_ABSENT`仅表示该帧候选中没有手机，不能转成整个事件排除。无视频动作或拍屏机会判断，不与Mage的20帧生产链路直接计算跨模型准确率或速度倍数。

## 来源与运行适配

- [官方仓库](https://github.com/om-ai-lab/VLX-Seek)，源码固定为`610afc7e7fda81f5c69a036b869ce86f756916e0`。
- [官方模型](https://huggingface.co/omlab/VLX-Seek-1.5-10B)，权重固定修订`6d1925f932ee6f0a8790b15420f403ee8f8e8f6a`。
- 原始BF16文件20,033,961,788字节，完整SHA256核验为`abbbc2d103d76bf00b032bb09a9b833ff089890a763c4874a7f28f98c62af351`。下载、依赖和CPU预检查期间不暂停Mage。
- 试验使用bitsandbytes NF4、double quant，量化249个语言线性模块，包括lm_head；不是官方GGUF发布，也不是作者认证的量化精度。
- 主视觉、辅助视觉、HFRE和两个投影器不量化。初始参数使用BF16；辅助视觉输入归一化保持FP32，区域模块内部沿用官方归一化等精度转换。
- 独立依赖：torch 2.11.0+cu128、torchvision 0.26.0+cu128、torchaudio 2.11.0+cu128、transformers 5.13.0、accelerate 1.4.0、bitsandbytes 0.49.2、timm 1.0.9、einops 0.6.1、safetensors 0.8.0。
- 与官方torch 2.10/torchvision 0.25要求有偏差；两处视觉FlashAttention2改为SDPA。没有安装flash_attn/fla/causal-conv1d，沿用可运行的Torch回退。以上限制禁止把结果泛化成官方BF16模型能力结论。
- 实际加载无missing/unexpected/mismatched keys，全GPU，无CPU/disk权重offload。量化尺度、激活、驱动内存必须实际测量，不能只用静态权重估算宣称可运行。

## 输入与有效结果

[官方接口](https://github.com/om-ai-lab/VLX-Seek/blob/main/docs/inference_zh.md)使用单图像和像素坐标候选框；本次采用默认手机检测任务、中文类别“手机”、temperature=0、max_new_tokens=96。图片不绘制框，框坐标由区域接口传入；红框预览仅供人工核对，不传模型。实际响应均7个token、格式合法、未截断。

冻结样本沿用此前手机真假试验：从原生产ROI/JPEG92像素导出native448无框PNG，文件SHA及逐像素一致性核对通过。候选框按实际取整缩放和letterbox偏移映射，不把缺少提议视为没有手机。

| 样本 | 人工画面核对 | 冻结帧序号 | 结果 | 秒数 |
| --- | --- | ---: | --- | ---: |
| p03 | 真手机 | 9 | PHONE | 6.108 |
| n03 | 深色饮杯 | 9 | FRAME_ABSENT | 0.550 |
| n01 | 包装／纸片 | 9 | PHONE（误认） | 0.543 |
| n02 | 纸包／包装 | 9 | PHONE（误认） | 0.557 |
| n07 | 纸质材料 | 0 | PHONE（误认） | 0.568 |
| p02 | 真手机 | 0 | PHONE | 0.542 |
| p05 | 真手机 | 9 | PHONE | 0.549 |
| p11 | 真手机 | 9 | PHONE | 0.552 |

首张6.108秒包含首次运行开销，后7张均值0.5516秒，8张均值1.2461秒。以上是已驻留模型的单帧短答案耗时，不含下载、冷导入、模型加载和Mage恢复，也不是整段事件延迟。

8/8条输出均记录主视觉、辅助视觉、区域编码器各执行1次。实测CUDA累计峰值allocated 7.8624GiB、reserved 8.6016GiB；2秒监督轮询观察到GPU总占用最高9287MiB，轮询不保证捕获绝对瞬时峰值。只有约1GB总显存余量，没有验证更大分辨率、更长输出或并行加载Mage。

## 运行保护与早期诊断失败

试验是throwaway feasibility spike，适配代码只保存在私有staging／本机output，不接入生产。生产源码、依赖、提示词、屏幕标定、事件结果和人工标签均未修改；网页及采集服务没有重启。

共享GPU测试会暂停Mage复核，不能描述为复核零中断。每次先验证签名health和前台，等待正在执行的production结束；systemd硬超时600秒、主存上限20GiB、CPU配额800%、KillMode=mixed、ExecStopPost启动Mage，脚本内部480秒预算、持续前台监督、finally恢复Mage。模型由zty运行；root监督仅使用独立标准库代码，禁用字节码且不导入线上受信包。

本轮发生4次早期中止后，第5次才完成有效8条：

1. A：全权重加载成功，但诊断代码假设存在hf_device_map属性，在推理前退出。
2. B：全权重加载成功，但loading_info包含集合，诊断JSON记录失败，在推理前退出。
3. C：首张实际推理返回，但区域模块普通forward钩子没有计数，校验中止；当时未保留原回答，不计有效成绩。
4. D：定位为官方HFRE重写__call__绕过普通forward钩子。主／辅助视觉各执行1次，原始真手机回答正确，区域普通钩子计数0；该诊断响应仍不计正式成绩。
5. E：透明包装实际HFRE调用后计数；8条均三路执行，结果完整保存，单元成功结束。

没有把4次中止解释为模型OOM或官方兼容性故障。A/B是实验记录错误，C/D是实验审计方式错误；这些错误造成额外暂停和冷启动，有实际复核影响。每次自动请求恢复后另行签名验证，不以systemd active代替模型ready。

E于10:59:30停Mage，11:00:36请求恢复，控制周期66.45秒；之后仍需机械盘冷加载和预热，实际复核暂停长于66秒。E采集监督全程8路在线，观察FPS 79.5626–80.3622，alerts=[]；没有声称采集FPS证明复核不中断。

CPU针对性测试19项通过，含坐标、帧选择、保守解析、集合序列化、失败恢复及自定义调用计数；另验证真实官方prepare接口首轮保留图像／框，后续缓存解码不重复编码。测试通过不等于模型判别准确率。

## 后续建议

保留Mage线上运行。若继续，可先独立比较相同候选的手部紧裁剪／更高有效像素及中性区域分类问题，记录纸包与书册混淆，再扩大真手机正样本与跨帧回归；本轮未执行这些额外实验。没有证据支持仅凭这8帧启用事件过滤或判断手机镜头能否拍到屏幕。

权重、图片、预览、manifest、原始响应、日志和临时代码仅私有保存，不提交Git；Git只保存本说明。

## 恢复验收

11:04后续签名检查确认Mage ready=true、内存压力false，生产接纳6条，已有完成请求累计推理67.595秒、最近已完成8.948秒，production_errors=0、offline_errors=0，仍有一条生产任务运行。第一阶段提示词、v31二阶段注册表与证据版本保持原值。网页running、8路在线、80.055FPS、alerts=[]，对外地址仍为`192.168.104.53:8767`。这些计数仅是本次重启以来，不是全历史稳定性保证。

最后一次Mage从10:59:30停止到11:03后签名ready约4分多钟，加上此前4次诊断失败引起的恢复，整个试验的复核影响大于最终66秒GPU控制周期；采集保留运行不等于复核零中断。本轮没有再次触发root缓存所有者问题，不放宽线上包校验。

有效E执行脚本SHA256为`cebd89330bff30329d4eea9130290c4be470c7b313e1372a72017eb1e16d9b5a`，NF4加载器`31aac2f4ffb9d3257de2fa3cdb10aacd814bfbfac5b4afb50e7280335c268c04`，区域调用计数器`8c966414b059485b649cf445f1ebefbca4c85066e27ad06db1693089ce1c6a0e`；服务器与本机私有副本一致。原始数据保存于本机`output/vlx-seek-10b-20260917/`及服务器私有staging，没有上传监控数据。
