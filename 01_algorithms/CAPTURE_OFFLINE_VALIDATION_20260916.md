# 防拍二阶段离线验证记录（2026-09-16）

## 结论

本轮 5 个冻结历史事件已全部完成，但结果不满足上线要求，因此二阶段结果暂不接入线上报警决策。

- 人工标签：2 个疑似拍屏正样本，3 个非拍屏负样本。
- Mage-VL 二阶段：5 个样本全部输出 `IMPOSSIBLE_AWAY_FROM_SCREEN`。
- 模型标签层面：2 个正样本均未识别，不能证明短视频动作复核有效。
- 最终保守聚合：5 个样本全部保留，正样本保留率 100%，误报削减率 0%。
- 原因：5 个事件的人员脚点均未落入对应屏幕已配置的可拍、遮挡或未知区域，多边形关系全部为 `unknown`；聚合策略因此没有采纳模型的排除结论。

下一步应先扩充并校准这 5 个摄像头的空间区域，再针对两个疑似拍屏正样本修订动作提示词或证据抽帧方式，之后重新运行同一冻结清单。当前结果不能用于评价真实误报削减能力。

## 运行与兼容性

- 冻结清单摘要：`a2f4ff6d45f0be600d4ae2dc0c2bc4b5f88f865b2fb8d9d46f1f096bc77a627c`
- 样本摄像头：`camera08`、`camera11`、`camera14`、`camera15`、`ffs_d4`
- 生产提示词版本：`600887293a7b364de10ee857979dd0713903914cc22212a66ee6c55169b7b993`
- 生产证据版本：`person-roi20-span5s-pre4-native-focus-temporal-early8-jpeg92-v20`
- 二阶段提示词版本：`043517e15a6d8d89e7fcea76f6ff4f499620f29385cd15cb53a5976695bd287c`
- 二阶段证据版本：`scene-person-phone-span5s-16f-jpeg92-v1`
- 调度规则：单模型、单并发；离线任务可预订当前任务后的一个时隙；已开始的离线任务不被前台中断；完成后优先归还前台；废弃预订自动失效；实际离线推理最多 2 次/分钟。
- 相关提交：`1c0617f`（离线时隙预订）、`2f1b946`（废弃时隙自动失效）。

## 验证证据

- 针对性及兼容性测试：68 项通过。
- 离线清单：5/5 完成，未产生 `UNCERTAIN` 结果。
- 平均二阶段延迟：12.68 秒。
- 验证结束后模型服务：就绪，无显存压力，生产复核正在运行，生产与离线错误计数均为 0。
- 验证结束后前台：`running`，8 路摄像头，约 79.9 FPS，0 告警。
- 对外网页地址保持不变：`192.168.104.53:8767`。

结果文件保留在隔离验证目录中，未纳入 Git；其中包含事件标识和运行路径，不在本记录中展开。

## 运行记录说明

结果文件中的暂停次数包含早期 30 秒静默策略、修复前取消、一次修复前 HTTP 错误，以及本轮高频忙碌轮询，不代表 415 次模型故障。最终有效完成记录为 5 条；历史取消记录 1 条，最终完成样本没有取消。
